<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TinyApp</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <header>
  </header>
  <main>
    <h1>Welcome to TinyApp</h1>
    <div class="grid-container">
      <% for(let property in urls) { %>
        <%=property%> =>
          <%= urls[property] %>
            <% let deleteURL = "/u/" + property + "/delete"; %>
              <% let modifyURL = "/urls/" + property; %>
                <form method="POST" class="grid-item1" action=<%=deleteURL %>>
                  <button id="button" class="grid-item" type="submit">DELETE</button>
                </form>
                <form method="GET" class="grid-item2" action=<%=modifyURL %>>
                  <button id="button" class="grid-item" type="submit">EDIT</button>
                </form>
                <% }; %>
    </div>
    <footer>
      <% include partials/_footer.ejs %>
    </footer>
  </main>
</body>

</html>